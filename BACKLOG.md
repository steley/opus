# Opus — Backlog / 演进备忘

> 自 2026-09 多轮评审/加固后，把「尚未做但从产品/合规/灾难恢复角度值得记忆」的事项集中记录于此，
> 与路由/安全等已在 repo 的文档区分，避免遗忘。已修项不重复列。
>
> 📌 重要修正：**Cloudflare Workers 免费版支持 Cron**（本会话已实测），
> `worker` 的 `scheduled` 每日清理（`wrangler.toml [triggers]` 17 3 * * *）是可用的，无需为此升级/架构改造。

## 优先级排序（按性价比）

### 🔴 高度建议（低成本高保障）
- [ ] **D1 数据备份习惯**：opus 全部内容与绑定系于单一 CF 账号；一旦误删/封号不可恢复且无本地副本。
      建议定期 `npx wrangler d1 export opus --remote --output backups/opus-<date>.sql`（并加入 .gitignore），
      或在 VPS 上留 sqlite 同步兜底。
- [ ] **读者端举报/联系方式**（详见下方「举报」小节）——匿名平台收到违规内容时的最低闭环反馈。规划中。
- [ ] **CF Web Analytics（可选，零成本/无 cookie）**：不追个体、契合「极简零外部资源」，用于观察真实访问
      （来源/慢页），替代目前只有 DNS 的裸跑。

### 🟠 产品边界 / 预期管理
- [ ] **「阅后即焚」的互联网边界**：销毁只发生在自有库；分享卡片快照、爬虫存档、Wayback 等无法收回。
      宜在产品文案里诚实告知（如分享到 IM 的旧卡片可能比正文长寿）。
- [ ] **发布者的自测盲区**：作者自己先看焚文应走 `/edit/:id`（编辑读取不触发焚）；直接 GET `/id` 即焚。
      目前无 UI 提示，宜加入帮助/提示。

### 🟡 演进（触发信号再动手）
- [ ] 违规内容刹车：收到第一条举报/投诉后，落地「删除能力」（服务端 HTTP + 预埋管理凭据；UI 后补）。
      DMCA/GDPR 合规需此闭环。
- [ ] 若公开后遇真人 spam / captcha 农场：防线顺序在内容层（URL 黑名单、频率、举报），而非入口层。
- [ ] 口令哈希版本化（pbkdf2-sha256$iter$salt$hash）：升 Paid/换算法时才需要，现在不必。
- [ ] 超大文章 CPU 是否真超限：用 `wrangler tail` 实测 100/200/500KB 后再定是否压 html 上限。
- [ ] 若真需视频直链：另行加 video/source + videoHosts 白名单（现为停用态）。
- [ ] 商品命名撞车（Opus 为通用词）与 hello@opus.cc 域名邮箱是否已开通 MX —— 早期可不管，出圈前注意。

## 「举报」— 设计思路（待定邮件通道）
目标：访客点击「举报」→ 发送邮件至 hello@opus.cc，主题默认包含文章链接，便于你直接从标题定位并处理。
可选通道（二选一，需确认）：
- **mailto 方案（零成本/零依赖/零新配置）**：`mailto:hello@opus.cc?subject=举报: https://opus.cc/<id>`，
  访客在其邮件客户端只需按发送；无需任何 API/第三方，适合极简、超低频。缺点：无邮件客户端者体验差。
- **邮件 API 方案（Resend/SendGrid/Mailgun）**：访客点按后由 Worker 直接代发（主题带链接），
  你无需访客发件；需注册+验证发信域名+API key 放 secret。免费额度对举报量足够。

正文可附简短的站内举报原因（可选下拉：垃圾广告/侵权/骚扰/其它）与文章元信息；工作区可加一个「文章 ID + 签名」可选项以便你核对。

## 安置点（供参考，未定稿）
选项：阅读页正文下方 meta 区；或每文右上角小按钮；或与既有入口(编辑/回顶)共存。为不破坏极简，
建议放在「文章操作/meta + 短 hint」而非常驻醒目；后续统一放到 footer/文章卡片可复用组件。

最后更新：2026-09（cron 修正 + 举报章节初稿）
